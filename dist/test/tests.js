(function( w, $ ) {
	"use strict";

	QUnit.module( "Defaults", {
		setup: function() {
			
		},
		teardown: function() {
			
		}
	});

	QUnit.test( "True", function( assert ) {
		assert.ok( true );
	});
}( this, jQuery ));
